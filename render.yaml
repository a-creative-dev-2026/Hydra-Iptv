services:
  - type: web
    name: hydra-iptv-v4
    env: python
    buildCommand: pip install -r requirements.txt
    # Let Render inject the PORT variable
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
